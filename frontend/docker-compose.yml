# frontend/docker-compose.yml
services:
  frontend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8080 # 👈 お隣のお城を外から叩くにゃ！
